<h1>show records</h1>

<table>
  <tr>
    <th>Lname</th>
    <th>Dob</th>
    <th>Choose</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @student1s.each do |student1| %>
  <tr>
    <td><%= student1.lname %></td>
    <td><%= student1.dob %></td>
    <td><%= student1.choose %></td>
    <td><%= link_to 'Show', student1 %></td>
    <td><%= link_to 'Edit', edit_student1_path(student1) %></td>
    <td><%= link_to 'Destroy', student1, confirm: 'Are you sure?', method: :delete %></td>
	
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Student1', new_student1_path %>
